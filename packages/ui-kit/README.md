# @meetgram/ui-kit

All the UI components of Meetgram.

### Usage:

- Install package:

  ```bash
    pnpm add @meetgram/ui-kit@latest
  ```

- Install dependencies:

  ```bash
  pnpm i
  ```

- Run storybook:
  ```bash
  pnpm dev
  ```
